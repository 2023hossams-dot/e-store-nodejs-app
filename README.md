# 🛍️ متجر إلكتروني حديث - E-Store Application

تطبيق متجر إلكتروني احترافي مبني بأحدث المواصفات والتقنيات الحديثة باستخدام **Node.js** و **Express.js** و **MongoDB**.

## ✨ الميزات الرئيسية

### 👥 إدارة المستخدمين
- ✅ التسجيل والدخول الآمن مع JWT
- ✅ إدارة الملف الشخصي
- ✅ عناوين متعددة للشحن
- ✅ قائمة الرغبات (Wishlist)
- ✅ تتبع سجل الطلبات
- ✅ تفعيل البريد الإلكتروني
- ✅ إعادة تعيين كلمة المرور

### 📦 إدارة المنتجات
- ✅ نظام فئات المنتجات
- ✅ فلترة متقدمة (السعر، الفئة، التقييم)
- ✅ بحث نصي كامل
- ✅ صور متعددة للمنتج
- ✅ نظام التقييمات والتعليقات
- ✅ المنتجات المميزة
- ✅ إدارة المخزون

### 🛒 سلة التسوق
- ✅ إضافة/حذف المنتجات
- ✅ تحديث الكميات
- ✅ حفظ السلة في قاعدة البيانات
- ✅ حساب الإجمالي التلقائي
- ✅ كوبونات الخصم

### 💳 الطلبات والدفع
- ✅ نظام الطلبات المتقدم
- ✅ حالات الطلب المختلفة
- ✅ معلومات الشحن والفواتير
- ✅ تتبع الشحنات
- ✅ سياسة الإرجاع
- ✅ حساب الضرائب تلقائياً
- ✅ تكاليف الشحن ديناميكية

### 🔐 الأمان
- ✅ تشفير كلمات المرور مع Bcrypt
- ✅ JWT للمصادقة
- ✅ تحديد معدل الطلبات (Rate Limiting)
- ✅ تطهير المدخلات
- ✅ رؤوس أمان HTTP
- ✅ قفل الحسابات بعد محاولات فاشلة

### 📊 لوحة التحكم (Admin)
- ✅ إدارة المنتجات
- ✅ عرض جميع الطلبات
- ✅ تحديث حالة الطلبات
- ✅ إدارة الفئات
- ✅ إدارة الكوبونات

## 🏗️ بنية المشروع

```
e-store-nodejs-app/
├── src/
│   ├── server.js                 # ملف السيرفر الرئيسي
│   ├── config/
│   │   └── database.js           # إعدادات قاعدة البيانات
│   ├── models/                   # نماذج Mongoose
│   │   ├── User.js
│   │   ├── Product.js
│   │   ├── Order.js
│   │   ├── Cart.js
│   │   ├── Category.js
│   │   ├── Review.js
│   │   └── Coupon.js
│   ├── controllers/              # معالجات الطلبات
│   │   ├── authController.js
│   │   ├── productController.js
│   │   └── orderController.js
│   ├── routes/                   # مسارات API
│   │   ├── authRoutes.js
│   │   ├── productRoutes.js
│   │   ├── orderRoutes.js
│   │   └── cartRoutes.js
│   └── middleware/               # وسائط Express
│       ├── auth.js
│       ├── errorHandler.js
│       ├── rateLimiter.js
│       └── security.js
├── views/                        # صفحات HTML
│   ├── layout.html
│   ├── index.html
│   ├── products.html
│   ├── cart.html
│   ├── checkout.html
│   ├── login.html
│   ├── register.html
│   ├── account.html
│   └── orders.html
├── public/                       # ملفات ثابتة
│   ├── css/
│   │   └── style.css
│   ├── js/
│   │   └── main.js
│   └── images/
├── .env                          # المتغيرات البيئية
├── .env.example                  # مثال على المتغيرات
├── package.json                  # المكتبات المعتمدة
└── README.md                     # هذا الملف
```

## 🚀 البدء السريع

### المتطلبات الأساسية
- **Node.js** (v14 أو أحدث)
- **MongoDB** (محلي أو عبر Cloud)
- **npm** أو **yarn**

### خطوات التثبيت

1. **استنساخ المستودع**
```bash
git clone <repository-url>
cd e-store-nodejs-app
```

2. **تثبيت المكتبات**
```bash
npm install
```

3. **إنشاء ملف البيئة**
```bash
cp .env.example .env
```

4. **تحديث متغيرات البيئة**
```env
NODE_ENV=development
PORT=3000
MONGODB_URI=mongodb://localhost:27017/estore
JWT_SECRET=your-secret-key-here
SESSION_SECRET=your-session-secret-here
```

5. **تشغيل السيرفر**

للتطوير (مع Nodemon):
```bash
npm run dev
```

للإنتاج:
```bash
npm start
```

## 📚 API Endpoints

### المصادقة (Auth)
```
POST   /api/auth/register          # التسجيل
POST   /api/auth/login             # الدخول
POST   /api/auth/logout            # الخروج
GET    /api/auth/me                # الملف الشخصي الحالي
PUT    /api/auth/profile           # تحديث الملف الشخصي
PUT    /api/auth/change-password   # تغيير كلمة المرور
```

### المنتجات (Products)
```
GET    /api/products                   # جميع المنتجات
GET    /api/products/:id               # منتج معين
GET    /api/products/slug/:slug        # منتج بالـ slug
GET    /api/products/featured          # المنتجات المميزة
GET    /api/products/category/:slug    # منتجات الفئة
GET    /api/products/search?q=         # البحث
POST   /api/products                   # إنشاء (Admin)
PUT    /api/products/:id               # تحديث (Admin)
DELETE /api/products/:id               # حذف (Admin)
```

### السلة (Cart)
```
GET    /api/cart                    # الحصول على السلة
POST   /api/cart/add                # إضافة منتج
POST   /api/cart/remove/:productId  # حذف منتج
PUT    /api/cart/update/:productId  # تحديث الكمية
POST   /api/cart/clear              # تفريغ السلة
```

### الطلبات (Orders)
```
POST   /api/orders                   # إنشاء طلب جديد
GET    /api/orders/my-orders         # طلباتي
GET    /api/orders/:id               # تفاصيل الطلب
PUT    /api/orders/:id/cancel        # إلغاء الطلب
GET    /api/orders                   # جميع الطلبات (Admin)
PUT    /api/orders/:id/status        # تحديث حالة الطلب (Admin)
```

## 🔑 المصادقة

يستخدم التطبيق **JWT** للمصادقة:

```javascript
// إضافة التوكن في رؤوس الطلب
Authorization: Bearer <your-token>
```

## 📦 المكتبات المستخدمة

```json
{
  "express": "^4.18.2",
  "mongoose": "^8.0.0",
  "jsonwebtoken": "^9.1.0",
  "bcryptjs": "^2.4.3",
  "express-validator": "^7.0.0",
  "cors": "^2.8.5",
  "dotenv": "^16.3.1",
  "express-session": "^1.17.3",
  "connect-mongo": "^5.1.0"
}
```

## 🧪 اختبار الـ API

يمكن استخدام **Postman** أو **cURL** لاختبار الـ API:

```bash
# تسجيل مستخدم جديد
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "firstName": "أحمد",
    "lastName": "محمد",
    "email": "user@example.com",
    "password": "password123",
    "phone": "+966501234567"
  }'

# الدخول
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123"
  }'
```

## 🎨 واجهة المستخدم

### الصفحات المتاحة
- 🏠 **الصفحة الرئيسية** - عرض المنتجات المميزة والفئات
- 📦 **صفحة المنتجات** - عرض جميع المنتجات مع الفلترة والبحث
- 🔍 **تفاصيل المنتج** - معلومات كاملة وتقييمات المنتج
- 🛒 **سلة التسوق** - إدارة المنتجات المختارة
- 💳 **الدفع** - إكمال عملية الشراء
- 👤 **حسابي** - إدارة البيانات الشخصية
- 📋 **طلباتي** - عرض سجل الطلبات

## 🔒 التحكم في الوصول

- **User**: مستخدم عادي
- **Admin**: مسؤول النظام

## 📈 تحسينات الأداء

- ✅ الفهرسة على قاعدة البيانات
- ✅ البحث النصي الكامل
- ✅ تحديد معدل الطلبات
- ✅ معالجة الأخطاء الشاملة
- ✅ رسائل خطأ واضحة

## 📝 ملاحظات مهمة

1. **تغيير مفاتيح الأمان**: غير قيم `JWT_SECRET` و `SESSION_SECRET` قبل النشر
2. **قاعدة البيانات**: استخدم MongoDB Atlas لـ Production
3. **HTTPS**: استخدم HTTPS في الإنتاج
4. **متغيرات البيئة**: احفظ `.env` آمناً ولا تشاركها

## 🐛 معالجة الأخطاء

يتم معالجة الأخطاء بشكل موحد مع رسائل واضحة:

```javascript
{
  "success": false,
  "message": "رسالة الخطأ"
}
```

## 📧 التواصل والدعم

للإبلاغ عن الأخطاء أو الاستفسارات، يرجى التواصل عبر:
- 📧 البريد الإلكتروني: info@store.com
- 📱 الهاتف: +966 50 000 0000

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة ISC.

## 👨‍💻 المساهمة

نرحب بالمساهمات! يرجى:
1. Fork المشروع
2. إنشاء فرع جديد
3. إجراء التغييرات
4. فتح Pull Request

---

**تم آخر تحديث**: ديسمبر 2024

✨ شكراً لاستخدامك متجرنا الإلكتروني!
