# 🚀 تشغيل متجرك الإلكتروني - خطوة بخطوة

## المتطلبات:
- ✅ Node.js v14+ 
- ✅ npm v6+
- ✅ MongoDB Atlas (أو MongoDB محلي)

---

## 📋 خطوات التشغيل:

### **الخطوة 1️⃣: افتح الطرفية (Terminal)**

#### على Windows:
- اضغط `Win + R`
- اكتب `cmd` أو `powershell`
- اضغط Enter

#### على Mac/Linux:
- اضغط `Ctrl + Alt + T` أو اضغط Cmd + Space وابحث عن Terminal

---

### **الخطوة 2️⃣: انتقل لمجلد المشروع**

```bash
cd /workspaces/e-store-nodejs-app
```

أو إذا كان في مكان مختلف:
```bash
cd /path/to/e-store-nodejs-app
```

---

### **الخطوة 3️⃣: ثبّت المكتبات المطلوبة**

جرب الأمر الأول أولاً:
```bash
npm install
```

**إذا حدث خطأ**، جرب:
```bash
npm install --legacy-peer-deps
```

**إذا استمر الخطأ**، جرب:
```bash
rm -rf node_modules package-lock.json && npm cache clean --force && npm install
```

⏳ **الانتظار**: هذه الخطوة قد تستغرق 2-5 دقائق

---

### **الخطوة 4️⃣: تحقق من .env**

تأكد أن ملف `.env` يحتوي على:
```
MONGODB_URI=mongodb+srv://hossams777910778_db_user:5aw3IhNH7cldnMf2@cluster0.kfz30vh.mongodb.net/?appName=Cluster0.mongodb.net/MyProStoreDB?retryWrites=true&w=majority
PORT=3000
NODE_ENV=development
```

---

### **الخطوة 5️⃣: شغّل الخادم**

```bash
npm run dev
```

### أو إذا لم يعمل:
```bash
node src/server.js
```

---

## ✅ سترى رسائل كهذه:

```
==================================================
🚀 متجرك الإلكتروني
==================================================
✓ MongoDB متصل بنجاح
✓ Server يعمل على: http://localhost:3000
✓ الضغط على Ctrl+C لإيقاف الخادم
```

---

## 🌐 افتح المتجر:

انسخ هذا الرابط وضعه في متصفحك:
```
http://localhost:3000
```

---

## 📱 الصفحات المتاحة:

| الصفحة | الرابط |
|--------|--------|
| 🏠 الرئيسية | `/` |
| 📦 المنتجات | `/products` |
| 🛒 سلة التسوق | `/cart` |
| 🔐 تسجيل الدخول | `/login` |
| ✍️ التسجيل | `/register` |
| 💳 الدفع | `/checkout` |
| 👤 الحساب | `/account` |
| 📋 الطلبات | `/orders` |

---

## 🔧 حل المشاكل الشائعة:

### ❌ **المشكلة: `npm ERR! code ETARGET`**
**الحل:**
```bash
npm install --legacy-peer-deps
```

---

### ❌ **المشكلة: `MongoDB connection failed`**
**الحل:**
1. تأكد من صحة `MONGODB_URI` في `.env`
2. تأكد من أن MongoDB Atlas نشط
3. تحقق من اتصالك بالإنترنت

---

### ❌ **المشكلة: `Port 3000 already in use`**
**الحل:**
استخدم منفذ مختلف:
```bash
PORT=3001 npm run dev
```

---

### ❌ **المشكلة: `Cannot find module`**
**الحل:**
```bash
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

---

## 🎯 البقية هي سهلة!

بعد التثبيت الناجح، المشروع جاهز بـ 100%:
- ✅ قاعدة بيانات MongoDB متصلة
- ✅ API مع 30+ endpoint
- ✅ صفحات فرونتند جاهزة
- ✅ نظام مصادقة (Authentication)
- ✅ نظام حماية (Security)

**استمتع بمتجرك الإلكتروني! 🎉**
