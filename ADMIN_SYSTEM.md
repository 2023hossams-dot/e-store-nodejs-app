## 🔐 نظام إدارة المشرفين

تم إضافة نظام منفصل وآمن لإدارة المشرفين مع صفحة تسجيل دخول خاصة منفصلة عن المستخدمين العاديين.

### 📋 المميزات

✅ **صفحة تسجيل دخول منفصلة** للمشرفين  
✅ **بيانات اعتماد خاصة** (اسم مستخدم + كلمة مرور)  
✅ **لوحة تحكم متقدمة** لإدارة المستخدمين والمنتجات والمشرفين  
✅ **نظام صلاحيات متقدم** (super_admin و admin)  
✅ **حماية عالية** مع تشفير كلمات المرور و JWT  
✅ **تسجيل الأنشطة** (Activity Log)  
✅ **حماية من محاولات الدخول غير المصرح بها**

---

## 🚀 البدء السريع

### 1️⃣ البيانات الافتراضية للمشرف

عند تشغيل التطبيق للمرة الأولى، يتم إنشاء مشرف افتراضي تلقائياً:

```
اسم المستخدم: admin
كلمة المرور: Admin@123456
البريد الإلكتروني: admin@estore.com
```

### 2️⃣ الوصول لصفحة تسجيل الدخول

```
http://localhost:3000/admin/login
```

### 3️⃣ تسجيل الدخول

أدخل بيانات المشرف الافتراضية وسيتم تحويلك إلى لوحة التحكم.

### 4️⃣ لوحة التحكم

من لوحة التحكم يمكنك:
- ✅ عرض إحصائيات المتجر
- ✅ إدارة المستخدمين (عرض، تعديل، حذف)
- ✅ إدارة المنتجات (إضافة، تعديل، حذف)
- ✅ إدارة المشرفين الآخرين
- ✅ تحديث ملفك الشخصي
- ✅ تغيير كلمة المرور

---

## 📁 البنية الملفية الجديدة

```
src/
├── controllers/
│   └── adminController.js          ← منطق إدارة المشرفين
├── routes/
│   └── adminRoutes.js              ← مسارات API المشرف
├── models/
│   └── Admin.js                    ← نموذج قاعدة البيانات
├── middleware/
│   └── auth.js                     ← (محدث) التحقق من المشرفين
└── config/
    └── adminInit.js                ← إنشاء المشرف الافتراضي

views/
├── admin-login.html                ← صفحة تسجيل دخول المشرف
└── admin-dashboard.html            ← لوحة التحكم
```

---

## 🔌 API المشرف

### 1. تسجيل الدخول
```
POST /api/admin/login
Content-Type: application/json

{
  "username": "admin",
  "password": "Admin@123456"
}

Response:
{
  "success": true,
  "message": "تم تسجيل الدخول بنجاح",
  "token": "jwt_token_here",
  "admin": {
    "id": "...",
    "username": "admin",
    "email": "admin@estore.com",
    "fullName": "المشرف الرئيسي",
    "role": "super_admin",
    "permissions": [...]
  }
}
```

### 2. الحصول على الملف الشخصي
```
GET /api/admin/me
Authorization: Bearer {token}
```

### 3. تحديث الملف الشخصي
```
PUT /api/admin/profile
Authorization: Bearer {token}
Content-Type: application/json

{
  "email": "new@email.com",
  "fullName": "اسم جديد",
  "phone": "+966..."
}
```

### 4. تغيير كلمة المرور
```
PUT /api/admin/change-password
Authorization: Bearer {token}
Content-Type: application/json

{
  "oldPassword": "Admin@123456",
  "newPassword": "NewPassword@123",
  "confirmPassword": "NewPassword@123"
}
```

### 5. إدارة المستخدمين
```
GET /api/admin/users                    ← قائمة المستخدمين
GET /api/admin/users/:id                ← بيانات مستخدم واحد
PUT /api/admin/users/:id                ← تحديث مستخدم
DELETE /api/admin/users/:id             ← حذف مستخدم
```

### 6. إدارة المنتجات
```
GET /api/admin/products                 ← قائمة المنتجات
POST /api/admin/products                ← إضافة منتج جديد
PUT /api/admin/products/:id             ← تحديث منتج
DELETE /api/admin/products/:id          ← حذف منتج
```

### 7. إدارة المشرفين (للمشرف العام فقط)
```
GET /api/admin/admins                   ← قائمة المشرفين
POST /api/admin/admins                  ← إضافة مشرف جديد
PUT /api/admin/admins/:id               ← تحديث مشرف
DELETE /api/admin/admins/:id            ← حذف مشرف
```

### 8. لوحة التحكم
```
GET /api/admin/dashboard
Authorization: Bearer {token}

Response:
{
  "success": true,
  "stats": {
    "totalUsers": 45,
    "totalProducts": 120,
    "totalOrders": 30
  }
}
```

### 9. تسجيل الخروج
```
POST /api/admin/logout
Authorization: Bearer {token}
```

---

## 🔒 الأمان والحماية

### شهادة JWT
- يتم التحقق من توكن JWT لكل طلب
- الصلاحية: 7 أيام (قابلة للتعديل)
- سر التوكن: `JWT_SECRET` في `.env`

### تشفير كلمات المرور
- استخدام bcrypt مع salt 10
- لا يتم تخزين كلمات المرور بشكل مباشر

### محاولات الدخول الخاطئة
- بعد 5 محاولات فاشلة: قفل الحساب لمدة 30 دقيقة
- تتبع تلقائي لمحاولات الدخول

### تسجيل الأنشطة (Activity Log)
- كل إجراء يتم تسجيله (تسجيل دخول، تحديثات، حذف، إلخ)
- آخر 100 نشاط يتم حفظها

---

## 👥 نظام الأدوار والصلاحيات

### 1. المشرف العام (super_admin)
- جميع الصلاحيات
- إدارة المشرفين الآخرين
- إنشاء وحذف الحسابات الإدارية

### 2. المشرف العادي (admin)
- صلاحيات محدودة حسب الأذونات المخصصة له
- الصلاحيات الممكنة:
  - `manage_users` - إدارة المستخدمين
  - `manage_products` - إدارة المنتجات
  - `manage_orders` - إدارة الطلبات
  - `manage_categories` - إدارة التصنيفات
  - `manage_coupons` - إدارة القسائم
  - `view_reports` - عرض التقارير
  - `manage_admins` - إدارة المشرفين
  - `manage_notifications` - إدارة الإشعارات

---

## 🔧 التخصيص

### تغيير بيانات المشرف الافتراضي

عدّل الملف `src/config/adminInit.js`:

```javascript
const defaultAdmin = await Admin.create({
  username: 'your_username',
  email: 'your@email.com',
  password: 'YourPassword@123',
  fullName: 'اسمك الكامل',
  phone: '+966...',
  // ...
});
```

### تغيير صلاحية JWT

عدّل `.env`:
```
JWT_SECRET=your-secure-secret-key
JWT_EXPIRE=7d
```

---

## 📱 التخزين المحلي (LocalStorage)

عند تسجيل الدخول، يتم حفظ:
```javascript
localStorage.setItem('adminToken', token);
localStorage.setItem('adminUser', JSON.stringify(admin));
```

---

## ⚠️ نقاط مهمة

1. **غيّر كلمة المرور الافتراضية** عند أول دخول
2. **لا تشارك بيانات الدخول** مع أشخاص غير موثوقين
3. **احتفظ بـ JWT_SECRET آمناً** في الإنتاج
4. **استخدم HTTPS** في بيئة الإنتاج

---

## 🐛 استكشاف الأخطاء

### لا يمكن تسجيل الدخول
- تحقق من صحة البيانات
- تأكد من اتصالك بـ MongoDB
- تحقق من أن الحساب مفعّل

### الحساب مقفول
- انتظر 30 دقيقة أو
- اطلب من super_admin تفعيله

### التوكن منتهي الصلاحية
- سجّل الدخول مجدداً

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشاكل، تحقق من:
- سجلات الأخطاء في Terminal
- قاعدة البيانات MongoDB
- متغيرات البيئة في `.env`

---

تم بناء نظام المشرف بمعايير أمان عالية وسهولة الاستخدام! 🎉
