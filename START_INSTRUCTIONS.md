# 🚀 تشغيل المشروع - شرح مفصل

## الطرق المختلفة للتشغيل:

### الطريقة 1: الأمر المباشر (الأسهل)
```bash
npm start
```

### الطريقة 2: مع Hot-Reload (للتطوير)
```bash
npm run dev
```

### الطريقة 3: باستخدام Node مباشرة
```bash
node src/server.js
```

### الطريقة 4: باستخدام Script
```bash
bash run-server.sh
```

---

## ✅ ستظهر الرسائل التالية عند النجاح:

```
============================================================
🚀 متجرك الإلكتروني
============================================================
✓ Server يعمل على: http://localhost:3000
✓ البيئة: development
✓ قاعدة البيانات: mongodb+srv://...
============================================================

✓ MongoDB متصل بنجاح
```

---

## 🌐 الروابط المتاحة:

| الرابط | الوصف |
|--------|-------|
| http://localhost:3000/ | الصفحة الرئيسية |
| http://localhost:3000/products | المنتجات |
| http://localhost:3000/cart | السلة |
| http://localhost:3000/login | تسجيل دخول |
| http://localhost:3000/register | تسجيل جديد |
| http://localhost:3000/api/health | فحص الصحة |

---

## ⚠️ إذا واجهت مشاكل:

### المشكلة: Port 3000 مستخدم
```bash
lsof -i :3000          # عرض العملية
kill -9 <PID>          # قتل العملية
# أو غيّر PORT في .env
```

### المشكلة: خطأ في MongoDB
```bash
# تحقق من MONGODB_URI في .env
# تأكد من توفر الإنترنت
# تأكد من أن IP الخادم مسموح
```

### المشكلة: المكتبات غير مثبتة
```bash
npm install
npm start
```

---

## 🔧 التشخيص:

```bash
# فحص شامل
bash diagnose.sh

# فحص المنفذ 3000
lsof -i :3000

# فحص المكتبات
npm ls --depth=0

# فحص Node.js
node --version
```

---

## 🎯 الخطوات السريعة:

1. **تأكد من المكتبات:**
   ```bash
   npm install
   ```

2. **شغّل الخادم:**
   ```bash
   npm start
   ```

3. **افتح المتصفح:**
   ```
   http://localhost:3000
   ```

4. **جرب الميزات:**
   - عرض المنتجات
   - البحث
   - التسجيل
   - السلة

---

## 📊 المعلومات المهمة:

**Host:** localhost  
**Port:** 3000 (أو قيمة PORT من .env)  
**Database:** MongoDB Atlas  
**Environment:** development (أو قيمة NODE_ENV من .env)  

---

## 🛑 لإيقاف الخادم:

```
اضغط: Ctrl + C
```

---

**استمتع بتطويرك! 🎉**
